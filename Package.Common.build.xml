<?xml version="1.0" encoding="utf-8" ?>
<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003" DefaultTargets="Package">

  <!-- 
  	****************************************
  	* IMPORTS
	****************************************
  -->
  <PropertyGroup>
  	<MSBuildCommunityTasksPath>$(MSBuildProjectDirectory)\Tools\MSBuildCommunityTasks</MSBuildCommunityTasksPath>
  	<MSBuildUmbracoTasksPath>$(MSBuildProjectDirectory)\Tools\MSBuildUmbracoTasks</MSBuildUmbracoTasksPath>
  </PropertyGroup>
  
  <Import Project="$(MSBuildCommunityTasksPath)\MSBuild.Community.Tasks.Targets" />
  <Import Project="$(MSBuildUmbracoTasksPath)\MSBuild.Umbraco.Tasks.Targets" />

  <!-- 
  	****************************************
  	* PROPERTIES
	****************************************
  -->
  <PropertyGroup>
	  <PackageVersion>1.2</PackageVersion>
	  <BuildConfig>Debug</BuildConfig>
  </PropertyGroup>

  <PropertyGroup>
	  <RootDir>$(MSBuildProjectDirectory)</RootDir>
	  <BuildDir>$(RootDir)\Build</BuildDir>
	  <PackageDir>$(RootDir)\Package</PackageDir>
  </PropertyGroup>

  <PropertyGroup>
    <ILMergeExe>$(MSBuildProjectDirectory)\Tools\ILMerge\ILMerge.exe</ILMergeExe>
  </PropertyGroup>
  
  <!-- 
  	****************************************
  	* TARGETS
	****************************************
  -->
  
  <!-- CLEAN ALL -->
  <Target Name="CleanAll" DependsOnTargets="Clean">
  	<RemoveDir Directories="$(PackageDir)" Condition="Exists('$(PackageDir)')" />
  	<MakeDir Directories="$(PackageDir)" />
  </Target>
  
  <!-- CLEAN -->
  <Target Name="Clean">
	<RemoveDir Directories="$(BuildDir)" Condition="Exists('$(BuildDir)')" />
	<MakeDir Directories="$(BuildDir)" />
  </Target>
  
  <!-- UPDATE ASSEMBLEY VERSION -->
  <Target Name="UpdateAssemblyInfo">
	<FileUpdate
        Encoding="ASCII"
        Files="$(ProjectDir)\Properties\AssemblyInfo.cs"
        Regex="AssemblyVersion\(&quot;.*&quot;\)\]" 
        ReplacementText="AssemblyVersion(&quot;$(PackageVersion).0.0&quot;)]" />
	<FileUpdate
        Encoding="ASCII"
        Files="$(ProjectDir)\Properties\AssemblyInfo.cs"
        Regex="AssemblyFileVersion\(&quot;.*&quot;\)\]" 
        ReplacementText="AssemblyFileVersion(&quot;$(PackageVersion).0.0&quot;)]" />
  </Target>
  
  <!-- COMPILE -->
  <Target Name="Compile" DependsOnTargets="UpdateAssemblyInfo">
	<MSBuild Projects="$(ProjectDir)\$(ProjectName).csproj" Properties="Configuration=$(BuildConfig);" />
  </Target>
  
  <!-- MANIFEST -->
  <Target Name="Manifest" DependsOnTargets="PrepairFiles">
	<ItemGroup>
      <ManifestFiles Include="$(BuildDir)\$(ProjectName)\**\*" Exclude="$(BuildDir)\$(ProjectName)\Package.xml" />
    </ItemGroup>
	<ManifestUpdate ManifestFile="$(BuildDir)\$(ProjectName)\Package.xml"
	  WorkingDirectory="$(BuildDir)\$(ProjectName)"
      PackageVersion="$(PackageVersion)"
	  Files="@(ManifestFiles)" />
  </Target> 
    
  <!-- PACKAGE -->
  <Target Name="Package" DependsOnTargets="Manifest">
	<ItemGroup>
      <PackageFiles Include="$(BuildDir)\$(ProjectName)\**\*.*" />
  	</ItemGroup>
	<Package ManifestFile="$(BuildDir)\$(ProjectName)\Package.xml"
       WorkingDirectory="$(BuildDir)\$(ProjectName)"
       OutputDirectory="$(PackageDir)"
	   Files="@(PackageFiles)" /> 
  </Target>
  
</Project>